# Used when build context is repo root and Dockerfile is `backend/Dockerfile`
# (e.g. `docker build -f backend/Dockerfile .`). Docker reads this from the context root.
.git
.github
.expo
node_modules
app
assets
components
constants
context
data
lib
scripts
*.md
.env
.env.*
